services:
  nowsay:
    build: .
    container_name: nowsay
    restart: unless-stopped
    ports:
      - "1080:3000"
    volumes:
      - nowsay-data:/app/data
    env_file:
      - .env
    environment:
      - DB_PATH=/app/data/nowsay.db

volumes:
  nowsay-data:
